language: go
sudo: false

go:
- 1.10.x
- 1.11.x
- 1.12.x
- master

branches:
 only:
  - master

# uncomment this line if your project needs to run something other than `rake`:
script:
  - go test artread/... -v